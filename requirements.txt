fastapi==0.140.0
uvicorn[standard]==0.51.0
# Dependabot meldet mehrere kritische/hohe CVEs für chromadb - u.a.
# CVE-2026-45829 ("ChromaToast", kritisch) sowie die GitHub-Alerts #1
# (kritisch, Code-Injection über /api/v2/.../collections), #6 (kritisch,
# Code-Injection über /api/v2/.../collections/{id}, braucht
# UPDATE_COLLECTION-Recht), #5 (hoch, fehlende Tenant-Isolation) und #4
# (hoch, SimpleRBACAuthorizationProvider prüft Tenant/DB/Collection nicht).
# ALLE betreffen ausschließlich den per HTTP exponierten Server-Modus
# (chromadb.HttpClient / `chroma run`) mit Tenants/Nutzer:innen/RBAC; wir
# nutzen ausschließlich chromadb.PersistentClient (eingebettet, kein
# Netzwerk-Server, keine Tenants, kein Auth-Layer, siehe app/vectorstore.py),
# daher für keine der gemeldeten CVEs ausnutzbar. Für keine ist ohnehin ein
# Fix verfügbar, auch die hier gepinnte Version ist die aktuell neueste - bei
# jedem chromadb-Update sowie bei jeder NEUEN Dependabot-Meldung zu chromadb
# zunächst prüfen, ob sie ebenfalls nur den HTTP-Server-Modus betrifft, bevor
# reflexhaft eine Migration/ein Downgrade angestoßen wird.
chromadb==1.5.9
sentence-transformers==5.6.1
anthropic==0.120.0
tiktoken==0.13.0
python-dotenv==1.2.2
pydantic==2.13.4
pytest==9.1.1
httpx==0.28.1
trafilatura==2.1.0
youtube-transcript-api==1.2.4
pypdf==6.16.1
pymupdf==1.28.0
python-multipart==0.0.32
openai==2.48.0
networkx==3.6.1
Pillow==12.3.0
